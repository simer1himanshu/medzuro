{%- capture logobar -%}
<style>
  #PageContainer .page-width { max-width:{{section.settings.width}}px }
  .filterbrand { margin-bottom:20px; align-items:center; flex-wrap:wrap; }
.filterbrand a { width:42px; padding:7px; font-weight:400; text-align:center; background-color:#fff; box-shadow:0 0 0 1px #eee; display:inline-block; }
.brandList .ttl, .brandList .list { padding:20px 10px; border-top:1px solid #eee; }
.brandList .ttl { width:100px; font-size:20px; font-weight:700; }
.brandList ul { column-count:4; }
.brandList ul a { display:inline-block; padding:5px; }
@media only screen and (max-width:1024px) {
.brandList ul { column-count:3; }
}
@media only screen and (max-width:766px) {
.brandList .ttl { width:50px; }
.brandList ul { column-count:2; }
}

.brand-logo-list .grid { margin:0 -5px 25px; }
.brand-logo-list .grid .gitem { padding:0 5px; margin-bottom:10px; }
.brand-logo-list .logo-bar__link { padding:10px; display:block; text-align:center; border:1px solid #eee; }
</style>
{% unless section.settings.breadcrumb %}{%- if settings.breadcrumb_pg -%}{%- render 'breadcrumb' -%}{%- else -%}<div class="topSpace"></div>{%- endif -%}{% endunless %}
<div class="page-width">
  {% unless section.settings.hide_ttl %}<h1 class="page-title">{{page.title}}</h1>{% endunless %}
  {% unless page.content == blank %}<div class="rte">{{ page.content }}</div>{% endunless %}
  
{%- if section.settings.alphaList -%}
	{%- if section.settings.navigation -%}
      <div class="filterbrand fl">
        <a href="#bra">A</a>
        <a href="#brb">B</a>
        <a href="#brc">C</a>
        <a href="#brd">D</a>
        <a href="#bre">E</a>
        <a href="#brf">F</a>
        <a href="#brg">G</a>
        <a href="#brh">H</a>
        <a href="#bri">I</a>
        <a href="#brj">J</a>
        <a href="#brk">K</a>
        <a href="#brl">L</a>
        <a href="#brm">M</a>
        <a href="#brn">N</a>
        <a href="#bro">O</a>
        <a href="#brp">P</a>
        <a href="#brq">Q</a>
        <a href="#brr">R</a>
        <a href="#brs">S</a>
        <a href="#brt">T</a>
        <a href="#bru">U</a>
        <a href="#brv">V</a>
        <a href="#brw">W</a>
        <a href="#brx">X</a>
        <a href="#bry">Y</a>
        <a href="#brz">Z</a>
        <a href="#br09">0/9</a>
      </div>
    {%- endif -%}

  {% assign counter = 0 %}
  {% for vendor in shop.vendors %}
  {% assign counter = counter | plus: 1 %}
  {% endfor %}
  {% assign counter_divided_by_3 = counter | divided_by: 3 | floor %}

  <div class="brandList fl">
    <div id="bra" class="ttl">A</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "a" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brb" class="ttl">B</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "b" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brc" class="ttl">C</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "c" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brd" class="ttl">D</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "d" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="bre" class="ttl">E</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "e" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brf" class="ttl">F</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "f" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brg" class="ttl">G</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "g" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brh" class="ttl">H</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "h" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="bri" class="ttl">I</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "i" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brj" class="ttl">J</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "j" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brk" class="ttl">K</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "k" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brl" class="ttl">L</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "l" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brm" class="ttl">M</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "m" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brn" class="ttl">N</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "n" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="bro" class="ttl">O</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "o" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brp" class="ttl">P</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "p" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brq" class="ttl">Q</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "q" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brr" class="ttl">R</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "r" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brs" class="ttl">S</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "s" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brt" class="ttl">T</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "t" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="bru" class="ttl">U</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "u" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brv" class="ttl">V</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "v" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brw" class="ttl">W</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "w" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brx" class="ttl">X</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "x" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="bry" class="ttl">Y</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "y" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="brz" class="ttl">Z</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "z" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
  <div class="brandList fl">
    <div id="br09" class="ttl">0/9</div>
    <ul class="list fl1">
      {% for vendor in shop.vendors %}
      {% assign vendorName = vendor | split: "" | first | downcase %}
      {% if vendorName == "0" or vendorName == "1" or vendorName == "2" or vendorName == "3" or vendorName == "4" or vendorName == "5" or vendorName == "6" or vendorName == "7" or vendorName == "8" or vendorName == "9" %}
      {% assign its_a_match = false %}
      {% capture my_collection_handle %}{{ vendor | handleize | strip | escape  }}{% endcapture %}
      {% assign my_collection_handle_stripped = my_collection_handle | strip | escape %}

      {% for collection in collections %}
      {% if my_collection_handle_stripped == collection.handle %}
      {% assign its_a_match = true %}
      {% endif %}
      {% endfor %}

      {% if its_a_match %}
      <li class="vendor-list-item"><a href="{{ routes.collections_url }}/{{ vendor | handleize }}">{{ vendor }}</a></li>
      {% else %}
      <li class="vendor-list-item">{{ vendor | link_to_vendor }}</li>
      {% endif %}
      {% endif %}
      {% endfor %}
    </ul>
  </div>
{%- endif -%}
</div>
{% if settings.show_sticky_header %}
<script>
window.addEventListener('DOMContentLoaded',function(){
    $('.filterbrand a').on('click', function(e){
      e.preventDefault();
      var targetTab = $(this).attr('href'),
        tabposition = $(targetTab).offset(),
        headerHt = $("#header").height(),
        totalSpace = headerHt + 20;
      $("html, body").animate({ scrollTop: tabposition.top - totalSpace }, 500);
    });
});
</script>
{% endif %}
{%- endcapture -%}
{{logobar | strip_newlines | remove: "  " | remove: "	" }}
{% schema %}
  {
    "name": "t:sections.brands_list.name",
    "class": "index-section",
    "settings": [
	   {
		"type":"checkbox",
		"id":"section_width",
		"label":"t:global.fullwidth",
		"default": false
	  },
      {
        "type": "range",
        "id": "width",
        "label": "t:sections.brands_list.container_width",
        "default":1200,
        "min": 800,
        "max": 1800,
        "step": 10,
        "unit": "px"
      },
	  {
      	"type": "checkbox",
        "id": "hide_ttl",
        "label": "t:sections.brands_list.hide_page_title",
        "default": false
      },
	  {
      	"type": "checkbox",
        "id": "breadcrumb",
        "label": "t:sections.brands_list.hide_breadcrumb",
        "default": false
      },
      {
        "type": "paragraph",
        "content": "t:sections.brands_list.alphabetically_list"
      },
      {
        "type": "checkbox",
        "id": "alphaList",
        "label": "t:global.display",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "navigation",
        "label": "t:sections.brands_list.navigation",
        "default": true
      }
    ]  
  }
{% endschema %}